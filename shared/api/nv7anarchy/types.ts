export interface Element {
    name: string,
    color: string,
    createdOn: number,
    creator: string,
    comment: string,
    parents: string[],
    uses: number,
    foundby: number,
    complexity: number,
}
